{% set number=cert_hosts.etcd | length %}
{
  "CN": "etcd",
  "hosts": [
    "127.0.0.1",
    {% for ip in cert_hosts.etcd %}
      {% if number == loop.index %}
        "{{ ip }}"
      {% else %}
        "{{ ip }}",
      {% endif %}
    {% endfor %}
  ],
  "key": {
      "algo": "rsa",
      "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Guangdong",
      "L": "Guangzhou",
      "O": "k8s",
      "OU": "System"
    }
  ]
}
